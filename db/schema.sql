DROP DATABASE IF EXISTS blog_posts; -- deletes database if it already exists --
CREATE DATABASE blog_posts; -- creates the new database to be used ==

-- USE blog_posts; -- uses the database that we just created --